# Twitter/X Launch Thread

Post as a thread. Each tweet separated below.

---

**Tweet 1 (hook):**
```
I don't trust AI outputs.

Here's what I built to fix that. 🧵
```

**Tweet 2 (problem):**
```
The problem: I use Claude, GPT-4, and Ollama locally.

When one says something confidently wrong, I have no idea until it costs me.

No verification. No visibility. No accountability.
```

**Tweet 3 (solution):**
```
So I built TrustLayer — a universal trust layer that wraps around ANY AI you already use.

Every response gets a trust score.

"This response is 87% verified. 2 claims could not be confirmed."

Works with Ollama, Claude, GPT, Gemini — anything.
```

**Tweet 4 (features):**
```
It also:

→ Tracks your AI spending in real-time across ALL providers
→ Compares models on YOUR actual tasks (not synthetic benchmarks)
→ Indexes your docs locally for offline AI with Ollama
→ Builds no-code workflows without touching a prompt

All local. All private.
```

**Tweet 5 (install + CTA):**
```
Open source. Local-first. One command:

  pip install trustlayer-ai && trustlayer server

Your prompts stay on your machine. No accounts. No telemetry.

GitHub: https://github.com/acunningham-ship-it/trustlayer

Star if this is something you've wanted ⭐
```

---

# Standalone tweet (for algo boost later)

```
Hot take: the AI tools people will trust in 2026 aren't the smartest ones.

They're the ones that tell you when they're wrong.

That's what TrustLayer does → https://github.com/acunningham-ship-it/trustlayer
```
